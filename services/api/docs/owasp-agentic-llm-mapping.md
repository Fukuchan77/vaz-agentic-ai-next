# OWASP Agentic AI / LLM Top 10 Mapping

Satisfies Req 16.1/16.2 (`.sdd/specs/001-agent-architecture-enhancements/spec.md`).
Maps each risk in the **OWASP Top 10 for LLM Applications (2025)** — the
canonical "LLM Top 10", which folds agentic-autonomy risk into LLM06 Excessive
Agency — to this project's concrete mitigating control, or to an explicit
acceptance/deferral where no dedicated control exists yet. Every row cites the
implementing module and, where one exists, its test file, so a reviewer can
verify the claim against running code rather than prose.

A second table below extends this to the **OWASP Top 10 for Agentic
Applications (2025-12)** — the "Agentic AI Top 10" (ASI01–ASI10) — added per
`docs/cross-repo-adoption-backlog.md` X-13. The two tables are deliberately
non-overlapping: the LLM Top 10 folds agentic-autonomy risk into a single
entry (LLM06 Excessive Agency), while the Agentic AI Top 10 is the risk
taxonomy for a system with goals, tool access, memory, and multi-step
autonomy — a different granularity, not a duplicate one.

This document owns the mapping only; each control is owned and maintained by
its own component (see `plan.md`'s Components section). Update this table
whenever a cited file's mitigating behavior changes materially, not on every
unrelated edit to that file.

| # | Risk | Status | Mitigating control | Citation |
|---|------|--------|---------------------|----------|
| LLM01 | Prompt Injection | Partial / accepted | No dedicated input-side prompt-injection filter exists. Blast radius is bounded instead: even a successful injection cannot escape the tool allow-list, approval hook, or token budget. | `app/agents/guardrails.py` (`run_guarded`, `_GuardedToolset.call_tool`); `tests/unit/agents/test_guardrails.py` |
| LLM02 | Sensitive Information Disclosure | Mitigated | Logfire scrubbing of `prompt`/`tool_input`/`tool_output` is enabled by default; disabling it (`log_sensitive_payloads=True`) emits an `AUDIT:` warning. SSE/tool-call payload models use `extra="forbid"` so an unlisted field cannot silently leak. `Principal` identity is a one-way hash of the API key — the key itself is never stored or logged. | `app/observability.py` (`configure_logfire`); `app/config/observability.py` (`log_sensitive_payloads`); `app/patterns/sse.py` (`_StrictEvent`); `app/security/principal.py` (`derive_principal_id`); `tests/unit/test_observability.py` |
| LLM03 | Supply Chain | Mitigated | `pip-audit` runs on every PR and nightly on a cron; a local `pip-audit` pre-commit hook and `gitleaks` block a commit that introduces a vulnerable dependency or a secret; Dependabot keeps the `uv` dependency tree and pinned GitHub Actions current weekly; all third-party Actions are pinned to a commit SHA. | `.github/workflows/pr.yml`; `.github/workflows/security.yml`; `mise.toml` (`tasks.audit`); `.pre-commit-config.yaml`; `.github/dependabot.yml` |
| LLM04 | Data and Model Poisoning | Partial / accepted | `POST /v1/rag/ingest` requires a valid API key, limiting who can write into the vector store. No content sanitization or anomaly scoring is applied to ingested text — deferred; this project does no model training/fine-tuning, so poisoning risk is scoped to the RAG corpus only. | `app/api/v1/rag.py` (`ingest`, `Depends(verify_api_key)`) |
| LLM05 | Improper Output Handling | Mitigated | Citations returned to the caller are workflow-constructed from the deterministically-ordered, truncated hits actually fed to the synthesis prompt (never model-asserted), so a citation can't reference a chunk outside the current run. `validate_citations`/`order_hits` enforce this as defense-in-depth; `EmptyCitationError`/`DanglingCitationError` map to HTTP 502 with a generic message (no id/content leakage). At the retry limit the workflow returns a degraded answer grounded only in the hits it actually has, never a free-form fallback. | `app/workflows/citation.py`; `app/workflows/corrective_rag.py` (`search`/`evaluate`/`synthesize`); `app/api/v1/rag.py` (exception mapping); `tests/unit/workflows/test_citation.py` |
| LLM06 | Excessive Agency | Mitigated | `run_guarded()` wraps every tool-capable agent run with a native `toolset.filtered` allow-list, an `approval_hook`, a pre-side-effect token-budget check, and a closed `StopReason` vocabulary (`disallowed_tool`/`denied`/`budget_exceeded`/`max_iterations`/`completed`) plus an audit trail — applied identically on both the non-streaming and SSE-streaming chat paths. Session ownership is bound to the calling `Principal`; `authorize_session` returns 403 on any cross-principal session-id reuse. | `app/agents/guardrails.py`; `app/security/principal.py`; `app/services/session_service.py`; `tests/unit/agents/test_guardrails.py`; `tests/unit/services/test_session_service.py` |
| LLM07 | System Prompt Leakage | Accepted | The system prompt contains no secrets or credentials by construction (session-signing keys, API keys, etc. are never interpolated into it). The `NativeOutput(ChatOutput)` structured-output schema (where the active model profile supports it) constrains the response shape, reducing incidental verbatim disclosure. If a leak did occur, LLM02's Logfire scrubbing still limits its blast radius in logs/traces. No dedicated leak-detection test exists — accepted given the prompt carries no sensitive content today. | `app/agents/chat_agent.py` (`_build_system_prompt`, `ChatOutput`) |
| LLM08 | Vector and Embedding Weaknesses | Partial / accepted | Ingestion requires authentication (same control as LLM04), limiting who can influence retrieval ranking. `query_with_scores()` exposes a per-hit relevance score and a stable `chunk_id` (`{source}::{ordinal:04d}`) for every retrieved hit, giving a post-hoc audit trail back to the ingestion batch that produced it. No defense against embedding-inversion or adversarial-perturbation attacks is implemented — deferred. | `app/stores/vector_store/` (`query_with_scores`); `app/models/rag.py` (`RetrievedHit`) |
| LLM09 | Misinformation | Partial / mitigated | The CRAG workflow widens retrieval (`rag_initial_k` → `rag_widened_k`) and retries evaluation before synthesizing, and terminates early with a canned response on zero hits rather than generating from nothing; every synthesized claim is grounded to a specific retrieved chunk id (see LLM05). The offline two-axis (Outcome/Behavior) LLM-judge eval suite against a golden dataset provides a regression signal for factuality-adjacent behavior, gated on `mise run evals` pre-push. | `app/workflows/corrective_rag.py`; `evals/graders.py`; `evals/runner.py`; `evals/golden/basic_qa.json` |
| LLM10 | Unbounded Consumption | Mitigated | SSE streams are capped at `sse_max_events`, each send has a `sse_send_timeout`, and idle periods emit a `sse_heartbeat_interval` heartbeat. `POST /v1/agent/chat` is wrapped in `asyncio.wait_for(chat_request_timeout)`. Native `UsageLimits` (`usage_request_limit`/`usage_total_tokens_limit`) plus a pre-tool-call budget re-check in `run_guarded` bound token/request usage per run. Every LLM-invoking route (`/v1/agent/chat`, `/v1/agent/stream`, `/v1/rag/query`) carries a stricter `llm_rate_limit` (default `30/minute`) than the global default, backed by Redis with an in-memory fallback. The RAG workflow itself has a `rag_workflow_timeout` ceiling independent of the per-LLM-call `llm_agent_timeout`. | `app/patterns/sse.py`; `app/api/v1/_stream.py`; `app/agents/guardrails.py`; `app/middleware/rate_limit.py` (`enforce_llm_rate_limit`); `app/config/` (`sse_max_events`, `sse_send_timeout`, `sse_heartbeat_interval`, `chat_request_timeout`, `usage_request_limit`, `usage_total_tokens_limit`, `llm_rate_limit`, `rag_workflow_timeout`, `llm_agent_timeout`); `tests/unit/api/v1/test_stream_lifecycle.py`; `tests/e2e/test_rate_limiting_enforcement.py` |

## OWASP Top 10 for Agentic Applications (2025-12) Mapping

| # | Risk | Status | Mitigating control | Citation |
|---|------|--------|---------------------|----------|
| ASI01 | Agent Goal Hijack | Partial / accepted | No dedicated hijack-detection exists (a manipulated objective isn't distinguished from a legitimate one). Blast radius is bounded the same way as LLM01: even a hijacked run can't escape the tool allow-list, approval hook, or token budget, and a stopped run persists no session history — only `stop_reason == "completed"` saves — so a hijacked trajectory can't poison a future turn via saved history. One specific hijack vector is closed structurally (X-9): `ChatRequest` has no `message_history` field and `extra="forbid"`, so a client cannot fabricate a prior turn to steer the agent's perceived context — the field simply doesn't exist to inject through, and an attempt is a 422. | `app/agents/guardrails.py` (`run_guarded`, `_GuardedToolset`); `app/api/v1/agent.py` (`stop_reason == "completed"` gate on `save_history`); `app/models/agent.py` (`ChatRequest`); `tests/unit/agents/test_guardrails.py`; `tests/unit/models/test_request_model_strictness.py` |
| ASI02 | Tool Misuse and Exploitation | Mitigated | Direct correlate of LLM06 below: a native `toolset.filtered` allow-list, an `approval_hook` for dangerous tools, and a pre-side-effect token-budget check gate every tool call. Refused tools stay **visible** in the model's schema (not hidden), so a hallucinated or exploit-attempt call still reaches `call_tool()` and is recorded, not silently dropped. | `app/agents/guardrails.py`; `tests/unit/agents/test_guardrails.py` |
| ASI03 | Identity and Privilege Abuse | Partial / accepted | Session ids are server-issued and HMAC-signed (`{principal.id}.{token}.{signature}`); `authorize_session` 403s on any malformed or cross-principal id via `secrets.compare_digest`. `Principal.id` is a one-way hash of the API key, never stored or logged. The accepted gap: `Settings.api_key` holds exactly one key today, so `POST /v1/rag/ingest` writes into the single process-wide vector store with **no principal binding** — a second API key would make this cross-principal corpus poisoning, and the module docstring flags it as required reading before that change. | `app/security/principal.py` (module docstring, `derive_principal_id`); `app/services/session_service.py` (`authorize_session`); `app/api/v1/rag.py` (`ingest`) |
| ASI04 | Agentic Supply Chain Vulnerabilities | Accepted | No real tool, plugin, or MCP-server surface exists yet to carry this risk — the only registered tool is `mock_web_search`, a stub returning canned data, itself double-guarded (`app_env != "production"` *and* `enable_mock_tools`, with the import deferred so mock code can't run even if misconfigured). `docs/tool-design-conventions.md` plus the `real-tool-conventions-guard` pre-commit hook force a conscious review the moment a real tool is added — revisit this row then, especially for any third-party plugin/MCP dependency. | `app/agents/chat_agent.py` (mock-tool gating); `app/agents/tools_mock.py`; `docs/tool-design-conventions.md`; `.pre-commit-config.yaml` (`real-tool-conventions-guard`) |
| ASI05 | Unexpected Code Execution | Accepted | No code-execution or shell/subprocess tool exists in this codebase at any status — the only tool returns a fixed stub string. Not applicable today; revisit the instant any tool is added that generates or runs code/commands. | `app/agents/tools_mock.py`; `docs/tool-design-conventions.md` ("Status: deferred") |
| ASI06 | Context Management and Retrieval Manipulation | Partial / accepted | Shares LLM04/LLM08/LLM09's control surface, viewed from the agentic angle (does poisoned or stale retrieved context change future agent behavior?). Ingestion requires authentication, limiting who can poison the corpus. The CRAG workflow doesn't trust a single retrieval pass — it widens the search and re-evaluates via a structured `RelevanceVerdict` — and every synthesized claim is grounded to a citation validated against the hits actually retrieved *this run* (`validate_citations`/`order_hits`), so stale or tampered context can't silently pass through as an unverifiable assertion. `RAGQueryRequest`'s `extra="forbid"` (X-9) closes the client-supplied-context vector specifically: there is no `context`/`hits` field to inject a pre-fabricated retrieval result through. | `app/api/v1/rag.py` (`ingest`); `app/workflows/corrective_rag.py` (`search`/`evaluate`/`synthesize`); `app/workflows/citation.py`; `app/models/rag.py` (`RelevanceVerdict`, `RAGQueryRequest`); `tests/unit/models/test_request_model_strictness.py` |
| ASI07 | Insecure Inter-Agent Communication | Accepted | Not applicable: this codebase runs exactly one agent (`chat_agent`) plus one code-defined workflow (`corrective_rag`, a fixed search/evaluate/synthesize path — not multiple cooperating agents). There is no agent-to-agent messaging protocol, no A2A/MCP peer, and no sub-agent delegation for this risk to apply to. Revisit if a genuine multi-agent construction is ever adopted. | `CLAUDE.md` ("Multi-agent adoption gate" note, X-16) |
| ASI08 | Cascading Failures | Mitigated | A timed-out LLM call returns a safe fallback **immediately with no retry** (a consistently slow provider isn't a transient failure to pile retries onto), while genuinely transient errors get bounded exponential backoff with jitter. The whole-workflow timeout is a *separate* ceiling from the per-call one, mapped to HTTP 504 independently. On shutdown, each store/client closes independently through `_close_quietly` (logs, doesn't raise) so one failing close can't block the rest; the background `cleanup_loop` swallows and logs any non-cancellation error so a transient store failure can't kill it. | `app/workflows/rag_llm.py` (`_run_agent_with_retry`); `app/api/v1/rag.py` (`asyncio.timeout` → 504); `app/lifespan.py` (`_close_quietly`, `cleanup_loop`) |
| ASI09 | Human-Agent Trust Exploitation | Partial / accepted | Every refused, denied, or budget-blocked tool attempt is recorded to an `AuditTrail` and shipped to the caller as `ChatResponse.audit` — a human reviewing a run sees the mechanical record of what was blocked and why, not only the model's own narrative of what happened. Grounded citations (ASI06 above) mean a synthesized answer's claimed sources are independently checkable against what was actually retrieved, not merely asserted. No dedicated detection exists for a persuasive-but-wrong model explanation talking an operator into approving something — the `approval_hook` itself is a caller-supplied callable, so its trustworthiness is outside this codebase's control. | `app/agents/guardrails.py` (`AuditTrail`); `app/models/agent.py` (`ChatResponse.audit`); `app/workflows/citation.py` |
| ASI10 | Rogue Agents | Mitigated | Every agent run is request-scoped and bounded by `max_iterations` plus a pre-side-effect token budget under a closed `StopReason` vocabulary — there is no standing/persistent agent process that could compromise, misalign, or drift over a long horizon between requests. Session history is TTL-bounded and actively trimmed on every save, so even a compromised session can't accumulate unbounded state across many turns. | `app/agents/guardrails.py` (`StopReason`, `run_guarded`); `app/stores/session_store/_trim.py`; `app/lifespan.py` (`cleanup_loop`, session TTL) |

## Notes on scope

- **"Mitigated"** means a concrete, tested control exists and is wired into
  the production path (not just a dev-only or conditional path).
- **"Partial / accepted"** means a real control reduces the risk's blast
  radius or likelihood but does not eliminate it, and the residual risk is
  knowingly accepted rather than silently unaddressed.
- **"Accepted"** means the risk is judged low today given the current design
  (e.g. no secrets in the system prompt) with no dedicated control — revisit
  if that assumption changes (e.g. the system prompt starts carrying
  sensitive context).
- Requirement 15's tool-design conventions (naming, pagination, lenient
  parsing) are a code-quality/reliability concern for future real tools, not
  a distinct OWASP Top 10 risk, and are tracked separately in
  `docs/tool-design-conventions.md`.
- The Agentic AI Top 10 table's status vocabulary (Mitigated / Partial /
  accepted / Accepted) reuses the same three definitions above — there is no
  separate "not applicable" status. A risk whose preconditions don't exist in
  this codebase today (ASI04, ASI05, ASI07) is recorded as **"Accepted"**:
  the same "revisit if that assumption changes" caveat applies, and each such
  row names the concrete change that would make it apply again.
